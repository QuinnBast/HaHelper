serverPort: 9997
persistence:
  enabled: false
# replicas: 1
# storageLocation:
# compactionIntervalSeconds:
cluster:
  multiMaster: false
  knownHosts:
    - hostname: localhost
      port: 9998
    - hostname: localhost
      port: 9997
    - hostname: localhost
      port: 9996