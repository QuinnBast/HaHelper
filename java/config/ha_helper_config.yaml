local:
  hostname: localhost
  port: 9998
cluster:
  clusterSize: 3
  multiMaster: false
  storage:
    fileBasedConfig:
      enabled: false
    memoryBasedConfig:
      enabled: true
  discoveryMethod:
    knownHosts:
      - hostname: localhost
        port: 9998
      - hostname: localhost
        port: 9997
      - hostname: localhost
        port: 9996